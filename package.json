{
  "name": "revoult",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "admin"
  ],
  "scripts": {
    "dev": "npx concurrently -k -n backend,frontend,admin -c blue,green,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix admin run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
